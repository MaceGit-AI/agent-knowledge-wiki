---
description: Scaffold the Karpathy 3-layer contracts (specs/, VERIFIER.md, ENVIRONMENT.md) into this repo
argument-hint: "[optional: stack hint or notes]"
---

Scaffold the Karpathy 3-layer development contracts into the current repository
using the **karpathy-init** skill.

Notes / stack hint (optional): $ARGUMENTS

Follow the skill: detect the stack from real markers and scripts, recall existing
verifier/environment recipes from the wiki, generate `specs/` + `VERIFIER.md` +
`ENVIRONMENT.md`, and wire them into `AGENTS.md` (append — never clobber existing
pointers). Show a diff for anything that already exists before changing it. Report
what was created vs appended and any placeholders to fill.
